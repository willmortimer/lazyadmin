[tools]
go = "1.25"
node = "25.2.0"

[env]
GOTOOLCHAIN = "local"

[tasks.dev]
description = "Run dev docker-compose stack"
run = "docker compose up --build"

[tasks.tui]
description = "Run TUI natively"
run = "go run ./cmd/lazyadmin"

